targets:
  $default:
    builders:
      drift_dev:
        options:
          # Keep full-precision UTC timestamps so incremental sync never
          # misses a row written within the same second.
          store_date_time_values_as_text: true
